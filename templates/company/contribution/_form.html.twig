<div class="row mt-3">
    <div class="col">
        <div class="card">
            <div class="card-header h5">Nouvelle déclaration</div>
            {{ form_start(form) }}
            <div class="card-body">
                {{ form_widget(form) }}
            </div>
            <div class="card-footer d-flex justify-content-center">
                <div class="hstack gap-3">
                    <a class="btn btn-danger btn-sm" href="{{ path('app_company_dashboard') }}">Retour</a>
                    <button class="btn btn-success btn-sm">{{ button_label|default('Valider') }}</button>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>





